let arr = [5, 3, 8, 1];

function filterRange(arr, i, j){
      return arr.filter((item) => {
        return item>=i && item<=j;
      })
}

let filtered = filterRange(arr, 1, 4);

console.log(filtered);

/**Second Problem */
function filterRangeInPlace(arr, a, b)
{   let result = [];

    

}
